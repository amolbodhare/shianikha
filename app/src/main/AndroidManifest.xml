<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shianikha">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.OTPVereificationActivty"></activity>
        <activity android:name=".activities.RegSixthPageActivity" />
        <activity android:name=".activities.RegFifthPageActivity" />
        <activity android:name=".activities.RegFourthPageActivity" />
        <activity android:name=".activities.RegThirdPageActivity" />
        <activity android:name=".activities.RegSecondPageActivity" />
        <activity android:name=".activities.RegisterationActivity" />
        <activity android:name=".activities.WalkThroughActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>